export enum RustFunctionName {
// dataSource service
    greet = "greet",
    connectMidi = "connect_midi_device",
    stopMidi = "stop_midi"
}

export enum RustEventsName {
    midiNote = "MIDI_NOTE"
}